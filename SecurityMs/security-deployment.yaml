apiVersion: apps/v1
kind: Deployment
metadata:
  name: security-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: security-app
  template:
    metadata:
      labels:
        app: security-app
    spec:
      containers:
      - name: security-app
        image: yusrarayyan/security-ms:v1.0
        ports:
        - containerPort: 8686